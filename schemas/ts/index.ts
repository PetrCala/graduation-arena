// v0 — barrel re-export of the data contract types for the web app.
// See types.ts. To be auto-generated from schemas/json once web tooling (K1) lands.

export type {
  Level,
  EvaluatorRole,
  Grade,
  Evaluator,
  RawThesis,
  ParsedThesis,
  EvaluatorStats,
} from "./types";
